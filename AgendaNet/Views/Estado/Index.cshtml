@model IEnumerable<AgendaNet.Models.EstadoModel>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.cod_estado)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.sigla_estado)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.nome_estado)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.cod_estado)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.sigla_estado)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.nome_estado)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
}
    </tbody>
</table>
